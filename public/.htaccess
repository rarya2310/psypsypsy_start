# Enable URL rewriting
<IfModule mod_rewrite.c>
  RewriteEngine On
  # Always serve index.html for any non-file requests (SPA routing)
  RewriteBase /
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
